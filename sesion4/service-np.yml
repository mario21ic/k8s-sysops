kind: Service
apiVersion: v1
metadata:
  name: nginx-np-svc
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - name: web
      nodePort: 32002 # Node
      port: 8080      # service interno como ClusterIP
      targetPort: 80  # Container
