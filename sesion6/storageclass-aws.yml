apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast-ssd
provisioner: kubernetes.io/aws-ebs  # Quien crea el PV
parameters:
  type: gp3
  fsType: ext4
reclaimPolicy: Delete  # Qué pasa con el PV cuando se borra el PVC
